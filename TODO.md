# TODO

- [x] Register `/permissions` command while preserving `/ssh-policy` command registration.
- [x] Implement UI-mode `/permissions` panel with exactly two toggles (SSH, Bash) and Save/Cancel actions.
- [x] Persist `/permissions` Save to JSON paths by scope (`global` default, `project` override).
- [x] Ensure Cancel path does not persist changes.
- [ ] Run real-world integration testing in a controlled environment and capture findings in `docs/`.
